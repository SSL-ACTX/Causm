[workspace]
members = [
    "crates/*",
   # "lsp"
]
resolver = "2"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
strip = true

[profile.dev]
opt-level = 1
strip = true
panic = "unwind"
lto = false
codegen-units = 256
