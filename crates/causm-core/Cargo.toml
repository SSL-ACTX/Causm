[package]
name = "causm-core"
version = "0.1.0-alpha.2"
edition = "2021"
license = "AGPL-3.0-only"

[dependencies]
bincode = "1.3"
serde = { version = "1.0", features = ["derive"] }
thiserror = "2.0.18"

[dev-dependencies]
serde_json = "1.0"
