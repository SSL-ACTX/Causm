@0ms: {
    foreign "libc.so.6" abi("C") {
        routine socket(&domain: i32, &type: i32, &protocol: i32) -> i32 taking 1ms
        routine close(&fd: i32) -> i32 taking 1ms
        routine shutdown(&fd: i32, &how: i32) -> i32 taking 1ms
        routine bind(&fd: i32, &addr_buf: i64, &addrlen: i32) -> i32 taking 1ms
        routine connect(&fd: i32, &addr_buf: i64, &addrlen: i32) -> i32 taking 5ms
        routine listen(&fd: i32, &backlog: i32) -> i32 taking 1ms
        routine accept(&fd: i32, &addr_buf: i64, &addrlen_buf: i64) -> i32 taking 50ms
        routine send(&fd: i32, &buf: i64, &len: i32, &flags: i32) -> i32 taking 10ms
        routine recv(&fd: i32, &buf: i64, &len: i32, &flags: i32) -> i32 taking 50ms
        routine sendto(&fd: i32, &buf: i64, &len: i32, &flags: i32, &dest_addr: i64, &addrlen: i32) -> i32 taking 10ms
        routine recvfrom(&fd: i32, &buf: i64, &len: i32, &flags: i32, &src_addr: i64, &addrlen_buf: i64) -> i32 taking 50ms
        routine htons(&port: i32) -> i32 taking 1ms
        routine inet_addr(&ip: string) -> i32 taking 1ms
        routine setsockopt(&fd: i32, &level: i32, &optname: i32, &optval: i64, &optlen: i32) -> i32 taking 1ms
        routine fcntl(&fd: i32, &cmd: i32, &arg: i32) -> i32 taking 1ms
    }
}
