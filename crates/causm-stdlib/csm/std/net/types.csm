@0ms: {
    type SocketAddr = struct {
        ip: string,
        port: i32
    }
    type TcpStream = struct decay_after 10000ms {
        fd: i32,
        is_connected: bool,
        peer_ip: string,
        peer_port: i32
    }
    type TcpListener = struct {
        bind_port: i32,
        fd: i32,
        is_listening: bool
    }
    type UdpSocket = struct {
        fd: i32,
        is_bound: bool,
        port: i32
    }
    type Timeval = struct {
        tv_sec: i64,
        tv_usec: i64
    }
}
