@0ms: {
    foreign "libc.so.6" abi("C") {
        routine clock_gettime(peek clk_id: i32, peek tp: struct) -> i32 taking 1ms
        routine nanosleep(peek req: struct, peek rem: struct) -> i32 taking 1ms
        routine time(peek tloc: i64) -> i64 taking 1ms
    }
}
