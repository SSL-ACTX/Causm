@0ms: {
    foreign "libc.so.6" abi("C") {
        routine clock_gettime(&clk_id: i32, &tp: struct) -> i32 taking 1ms
        routine nanosleep(&req: struct, &rem: struct) -> i32 taking 1ms
        routine time(&tloc: i64) -> i64 taking 1ms
    }
}
