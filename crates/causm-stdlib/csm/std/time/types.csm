@0ms: {
    type Timespec = struct {
        tv_nsec: i64,
        tv_sec: i64
    }

    type Instant = struct {
        nanos: i64,
        secs: i64
    }

    type Duration = struct {
        nanos_total: i64
    }
}
