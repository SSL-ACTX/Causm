@0ms: {
    routine Utf8.encode(&text: string) -> array taking _ => str_bytes(text)

    routine Utf8.decode(&bytes: array) -> string taking _ => to_str(bytes)

    routine Utf8.encode_len(&text: string) -> i32 taking _ => len(str_bytes(text))

    routine Utf8.byte_len(&bytes: array) -> i32 taking _ => len(bytes)
}
