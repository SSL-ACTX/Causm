@0ms: {
    type File = struct decay_after 5000ms {
        is_open: bool,
        path: string,
        fd: i32
    }
    type FileMetadata = struct decay_after 5000ms {
        is_file: bool,
        size_bytes: i64,
        readonly: bool,
        is_directory: bool
    }
    enum OpenMode {
        ReadOnly,
        WriteOnly,
        ReadWrite,
        Append,
        CreateNew,
        Truncate
    }
    enum SeekFrom {
        Start(i64),
        Current(i64),
        End(i64)
    }
    enum FileResult {
        Ok(int),
        Err(string)
    }
}
