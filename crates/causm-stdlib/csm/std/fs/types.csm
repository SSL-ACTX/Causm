@0ms: {
    type File = struct decay_after 5000ms {
        path: string,
        fd: i32,
        is_open: bool
    }
    type FileMetadata = struct decay_after 5000ms {
        is_directory: bool,
        size_bytes: i64,
        is_file: bool,
        readonly: bool
    }
    enum OpenMode {
        ReadOnly,
        WriteOnly,
        ReadWrite,
        Append,
        CreateNew,
        Truncate
    }
    enum SeekFrom {
        Start(i64),
        Current(i64),
        End(i64)
    }
    enum FileResult {
        Ok(int),
        Err(string)
    }
}
