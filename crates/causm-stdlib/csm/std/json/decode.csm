@0ms: {
    from "std/json/types" import *

    routine Json.decode(&text: string) -> JsonValue taking _ => json_parse(text)

    routine Json.parse(&text: string) -> JsonValue taking _ => json_parse(text)
}
