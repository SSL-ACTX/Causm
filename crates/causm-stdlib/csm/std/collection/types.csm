@0ms: {
    type Buffer = struct {
        capacity: i32,
        data: array,
        len: i32
    }

    type Stack = struct {
        capacity: i32,
        data: array,
        len: i32
    }

    type Queue = struct {
        capacity: i32,
        data: array,
        head: i32,
        len: i32,
        tail: i32
    }

    type RingBuffer = struct {
        capacity: i32,
        data: array,
        head: i32,
        size: i32,
        tail: i32
    }

    type BitSet = struct {
        bits: array,
        len: i32
    }

    interface Collection {
        routine len(&self: Collection) -> i32
        routine is_empty(&self: Collection) -> bool
        routine to_array(&self: Collection) -> array
    }
}
