@0ms: {
    isolate sensor_types {
        enum SensorMode {
            HighPrecision(int),
            LowPower
        }
        type SensorConfig = struct decay_after 200ms {
            threshold: int,
            sensor_id: int
        }
    }
}
