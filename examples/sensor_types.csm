@0ms: {
    isolate sensor_types {
        pub enum SensorMode {
            HighPrecision(int),
            LowPower
        }

        pub type SensorConfig = struct decay_after 200ms {
            sensor_id: int,
            threshold: int
        }
    }
}
